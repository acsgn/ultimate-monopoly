package Test;

import static org.junit.Assert.*;

import org.junit.Test;

import game.Board;
import game.Player;
import game.Pool;
import game.square.action.TaxRefund;

public class TaxRefundTest {
	
	TaxRefund taxRefund;
	/*@Test
	public void test() {
		taxRefund = new TaxRefund();
		//Player p1 = new Player(new Board());
		int money = p1.getMoney();
		taxRefund.executeWhenLanded(p1);
		assertEquals(p1.getMoney(), (Pool.getInstance().getAmount()/2) + money);
	}*/

}
